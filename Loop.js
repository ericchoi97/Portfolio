import java.util.Scanner;
import java.math.*;

/**
   This program prints the numbers from 21 to 284.
   Multiples of 3 print Buzz in their replacement;
   Fizz for multiples of 5;
   FizzBuzz for muliples of 15 (3x5).
   Structure is as follows:
   while print(ing) [range 21-284]
   the [15] multiples replace the respective [3] and [5] multiples.

   EC: for numbers replaced with words, output the word, the number of times the replaced number is divisible.
*/

public class Loop
{
   public static void main (String [] args)
   {
for(int i = 21; i <= 284; i++)
	{
		if (((i % 3) == true) && ((i % 5) == true)))
			System.out.print("FizzBuzz");
			timesPrinted++;
			int n;
			int b;
			int f;
			int f == (Math.min(n,m));
			int i = log(n) / log(3);
			Math.log(n) / Math.log(3);
			int i = log(m) / log(5);
			Math.log(m) / Math.log(5);
			if(timesPrinted == timesToPrint)
				{
			     timesToPrint++;
			     timesPrinted == int f;
				}

				/**
				N= log.number from base 3
				M= log.number from base 5
				B= base of logarithm, either 3
				C= base of logarithm,
				F= FEWER; the lesser between n,m; the lesser number between the aforementioned numbers of log.base

				Use this identity:

				logb(n) = log(n) / log(b)
				Where log can be a logarithm function in any base, n is the number and b is the base.
				For example, in Java this will find the base-2 logarithm of 256:

				Math.log(256) / Math.log(2)
				=> 8.0

				Math.log() uses base e, by the way. And there's also Math.log10(), which uses base 10.
				*/


		else if  ((i % 3) == true)
			System.out.print("Buzz");
		else if  ((i % 5) == true)
			System.out.print("Fizz");
		else System.out.print(i);
	}
System.out.println
  	}
}
